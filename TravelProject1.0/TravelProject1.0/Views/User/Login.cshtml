@model TravelProject1._0.Models.DTO.UserDTO
@section Styles
{
    <link href="~/css/login.min.css" rel="stylesheet" />
}

@{
    ViewData["Title"] = "會員登入";
}

<h1>會員登入</h1>

    <div id="app" class="pico">
        <form action="@Url.Action("Login")" method="post" class="login">
            <h1 class="font">登入</h1>
        
        <label asp-for="@Model.Email">帳號:</label>
        <input class="inputstyle" type="text" placeholder="請輸入帳號" asp-for="@Model.Email" />
        <span asp-validation-for="@Model.Email" class="text-danger"></span>
       
        <label  asp-for="@Model.Password">密碼:</label>
        <input class="inputstyle" type="Password" placeholder="請輸入密碼" asp-for="@Model.Password" />
        <span asp-validation-for="@Model.Password" class="text-danger"></span>
       
        <input class="submitstyle" type="submit" value="提交" />

        <input value="註冊" type="submit" />
        </form>
      </div>

@section Scripts {

    <script src="https://cdn.jsdelivr.net/npm/vue@2/dist/vue.js"></script>
    <script>
       
        let app = new Vue({
            data: {
               
            },
            el: "#app",
            computed: {
            },
            mounted: function () {
                let _this = this;
                fetch("")
                    .then(response => {
                        return response.json();
                    }).then(data => {
                        console.log(data);
                        _this.products = data;
                    })
            },
            methods: {

            }
        });
              // var app2 = Vue.createApp(app).mount("#app");
    </script>
}
